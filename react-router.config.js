/**
 * React Router configuration for the app.
 * @file Enables server-side rendering by default.
 */
export default {
  // Config options...
  // Server-side render by default, to enable SPA mode set this to `false`
  ssr: true,
};
